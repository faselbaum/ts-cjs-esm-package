{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "applications/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "npm": ">=8.0.0",
    "node": "=16.17.0"
  }
}